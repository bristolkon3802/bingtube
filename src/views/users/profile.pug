extends ../base
include ../mixins/video

block content
    div.profile__data
        if loggedInUser.avatarUrl === undefind 
            span 😊
        else 
            img(src="/"+user.avatarUrl).avatar
        h3=user.name
    div.video-grid
        each video in user.videos  
            +video(video)
        else 
            span.empty__message 비디오가 없습니다.